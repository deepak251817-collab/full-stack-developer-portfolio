# Copy this file to .env.local and paste your Convex deployment URL.
# Run `npx convex dev` (or `npm run convex:dev`) to create a deployment,
# then copy the URL it prints (https://<deployment>.convex.cloud).
VITE_CONVEX_URL=
